{% assign label=include.name|capitalize %}{label: "{{include.label|default:label}}", name: "{{include.name}}", widget: "{{ include.widget|default:"string"}}"{% if include.hint %}), hint:"{{include.hint}}"{% endif %}, required: false}